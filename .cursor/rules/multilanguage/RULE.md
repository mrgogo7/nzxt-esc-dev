---
Project Rule: Multi-Language (i18n) Compliance

- All user-visible text MUST use the i18n system.
- No hard-coded strings in UI components.
- Every new UI feature must include translations for all supported languages.
- If a translation is missing, English must be used as fallback.
- Language files are flat and one-file-per-language.

alwaysApply: true
---
